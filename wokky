<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="A neon-rich advanced bio page.">
    <title>Neon Lifestyle Bio</title>
    <style>
        /* Global Styles */
        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(45deg, #03001e, #7303c0, #ec38bc, #fdeff9);
            background-size: 400% 400%;
            animation: neon-gradient 10s ease infinite;
            color: #fff;
            text-align: center;
            overflow-x: hidden;
        }

        @keyframes neon-gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Container */
        .container {
            padding: 20px;
        }

        /* Title & Subtitle */
        .neon-title {
            font-size: 3em;
            margin: 0.5em 0;
            text-shadow: 0 0 5px #fff, 0 0 10px #f39, 0 0 20px #f39, 0 0 40px #ff00ff;
        }

        .neon-subtitle {
            font-size: 1.5em;
            margin-bottom: 1.5em;
            text-shadow: 0 0 5px #fff, 0 0 10px #39f, 0 0 20px #39f, 0 0 40px #00ffff;
        }

        /* Section Headings */
        .neon-heading {
            font-size: 2em;
            margin: 1em 0;
            text-shadow: 0 0 5px #fff, 0 0 10px #0ff, 0 0 20px #0ff, 0 0 40px #00ffff;
        }

        /* Bio Text */
        .bio-text {
            font-size: 1.2em;
            margin: 20px auto;
            max-width: 600px;
            line-height: 1.6;
        }

        /* Neon Links */
        .neon-links a {
            display: inline-block;
            font-size: 1.2em;
            margin: 10px 15px;
            color: #fff;
            text-decoration: none;
            padding: 10px 20px;
            border: 2px solid transparent;
            border-radius: 25px;
            transition: 0.3s;
            text-shadow: 0 0 5px #fff, 0 0 10px #0ff, 0 0 20px #0ff, 0 0 40px #00ffff;
        }

        .neon-links a:hover {
            border-color: #0ff;
            background: rgba(0, 255, 255, 0.2);
        }

        /* Footer */
        footer {
            margin-top: 40px;
            font-size: 0.9em;
            text-shadow: 0 0 3px #fff, 0 0 6px #f39;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1 class="neon-title">Welcome to My World</h1>
            <p class="neon-subtitle">Living the Neon Lifestyle</p>
        </header>
        <section class="bio">
            <h2 class="neon-heading">About Me</h2>
            <p class="bio-text">I'm [Your Name], a [Your Role/Passion] who loves all things neon, tech, and adventure. Join me as I light up the world with creativity and boldness.</p>
        </section>
        <section class="links">
            <h2 class="neon-heading">Find Me Online</h2>
            <div class="neon-links">
                <a href="https://example.com" target="_blank">Twitter</a>
                <a href="https://example.com" target="_blank">Instagram</a>
                <a href="https://example.com" target="_blank">GitHub</a>
            </div>
        </section>
        <footer>
            <p class="footer-text">Made with 💖 | © 2024 [Your Name]</p>
        </footer>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const title = document.querySelector('.neon-title');
            let glowToggle = true;

            setInterval(() => {
                glowToggle = !glowToggle;
                title.style.textShadow = glowToggle
                    ? '0 0 5px #fff, 0 0 10px #f39, 0 0 20px #f39, 0 0 40px #ff00ff'
                    : '0 0 3px #fff, 0 0 6px #f39, 0 0 12px #ff00ff';
            }, 1000);
        });
    </script>
</body>
</html>

